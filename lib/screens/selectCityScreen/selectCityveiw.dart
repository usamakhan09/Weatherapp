import 'package:flutter/material.dart';

class SelectCityVeiw extends StatelessWidget {
  const SelectCityVeiw({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
